<!--Login Page-->
<div class="login-page">      
        <div class="form-heading">
            <h1>Login</h1>
        </div>

        <div class="form-section">
            <form action="#">
                <div class="field input-field">
                    <input type="email" placeholder="Email"id="email-login">
                </div>

                <div class="field input-field">
                    <input type="password" placeholder="Password"id="password-login">
                    <i class='bx bxs-hide eye-icon'></i>
                </div>

                <div class="field button-field">
                    <button>Login</button>
                </div>
            </form>

            <div class="form-link">
                <span>Don't have an account?</span> <a href="views\signup.handlebars" class="link signup-link">Signup</a> 
            </div>
        </div> 

        <div class="line"></div>

        <div class="media-options">
           <a href="#" class="field facebook">
            <i class='bx bxl-facebook facebook-icon'></i>
            <span>Login with Facebook</span>
           </a> 
        </div>

        <div class="media-options">
            <a href="#" class="field gmail">
                <i class='bx bxl-gmail gmail-icon'></i>
             <span>Login with Gmail</span>
            </a> 
         </div>

    </div>

<script src="../public/js/login.js"></script>